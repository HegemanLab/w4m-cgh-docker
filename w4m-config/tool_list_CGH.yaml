# This is just a fragment to be appended to tool_list.yaml, which is documented here:
#    https://github.com/galaxyproject/ansible-galaxy-tools/blob/master/files/tool_list.yaml.sample

#filter
- name: w4mclassfilter
  owner: eschen42
  tool_panel_section_id: 'filter'
  tool_shed_url: https://toolshed.g2.bx.psu.edu
  install_tool_dependencies: True
  install_resolver_dependencies: True
  revisions:
  - '191a720488ce' # v0.98.1

